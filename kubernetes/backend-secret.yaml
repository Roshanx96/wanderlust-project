apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
  namespace: wanderlust
type: Opaque
## NOTE: This file is a template. Do NOT commit real secrets.
## Recommended: create this secret at deploy time using kubectl or your secret manager.
## Example (PowerShell):
## kubectl create secret generic backend-secret --namespace wanderlust \
##   --from-literal=MONGODB_URI='mongodb://mongo-service/wanderlust' \
##   --from-literal=REDIS_URL='redis://redis-service:6379' \
##   --from-literal=JWT_SECRET='your-jwt-secret'

stringData:
  # Plaintext placeholders. Replace values or create the secret with kubectl as shown above.
  # Prefer the stable ClusterIP services added in the manifests (mongo-service, redis-service)
  MONGODB_URI: "mongodb://mongo-service/wanderlust"
  REDIS_URL: "redis://redis-service:6379"
  JWT_SECRET: "REPLACE_WITH_REAL_JWT_SECRET"
