VITE_API_PATH="http://backend-service:31100" 

# Purpose: VITE_API_PATH stores the base URL for the backend API that the frontend calls (e.g., /api/posts).

#---------------------1--------------------------
# Current style (build-time bake) — what you have
# Docker build copies .env.docker into the image. Image contains .env.local and the frontend uses it when starting.

#---------------------2--------------------------
# Recommended k8s runtime injection (ConfigMap + Deployment env example)
# - ConfigMap:
# - apiVersion: v1
# - kind: ConfigMap
# - metadata:
# - name: frontend-config
# - namespace: wanderlust
# - data:
# - VITE_API_PATH: "http://backend-service:31100"

# Deployment snippet (frontend):
# - spec:
# - template:
# - spec:
# - containers:
# - - name: frontend
# - image: roshanx/wanderlust-frontend-beta:v1
# - env:
# - - name: VITE_API_PATH
# - valueFrom:
# - configMapKeyRef:
# - name: frontend-config
# - key: VITE_API_PATH